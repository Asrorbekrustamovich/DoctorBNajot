# ============================================================
# Doctor B Najot — HIS. Ishlab chiqarish (production) talablari.
# O'rnatish:  pip install -r requirements.txt
# ============================================================

# --- Asosiy freymvork ---
Django>=5.2.6,<6.0
djangorestframework>=3.16
django-filter>=24.3
django-environ>=0.12.0
django-htmx>=1.21.0
django-cors-headers>=4.4.0

# --- Ma'lumotlar bazasi (PostgreSQL) ---
# [binary] — kompilyator kerak emas, wheel bo'lib keladi.
# psycopg-pool: masofadagi baza uchun ulanishlar hovuzi.
psycopg[binary,pool]>=3.2.10

# --- Kesh va navbat (Redis + Celery) ---
redis>=5.2.1
django-redis>=5.4.0
celery>=5.5
django-celery-beat>=2.8

# --- Fayl / hujjat / eksport ---
Pillow>=11.3
reportlab>=4.4
openpyxl>=3.1.5

# --- Ovoz (Navbat tablosi — o'zbekcha TTS) ---
edge-tts>=6.1.9

# --- Ishlab chiqarish serveri (WSGI + statik fayllar) ---
gunicorn>=23.0.0
whitenoise[brotli]>=6.8.2

# --- Server yordamchilari ---
# Zaxira nusxa vaqtida baza bilan ishlash uchun (pg_dump muqobili emas,
# faqat holatni tekshirish uchun ishlatiladi).
setproctitle>=1.3          # gunicorn jarayon nomlari `ps` da tushunarli chiqadi
